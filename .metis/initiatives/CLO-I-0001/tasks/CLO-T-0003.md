---
id: entity-trait-definitions
level: task
title: "Entity trait definitions"
short_code: "CLO-T-0003"
created_at: 2026-03-16T15:59:28.615952+00:00
updated_at: 2026-03-16T17:59:08.094539+00:00
parent: CLO-I-0001
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: CLO-I-0001
---

# Entity trait definitions

*This template includes sections for various types of tasks. Delete sections that don't apply to your specific use case.*

## Parent Initiative

[[CLO-I-0001]]

## Objective

Define the 7 core entity traits in `src/domain/traits.rs` per CLO-S-0002. These traits define the behavioral contracts that entity structs implement.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `Entity` trait — id(), entity_type(), title(), created_at(), updated_at()
- [ ] `Activatable: Entity` — status(), set_status()
- [ ] `Taskable: Entity` — state(), transition(to) -> Result, valid_transitions() -> Vec<TaskState>
- [ ] `Extractable: Entity` — extraction_status(), source_span(), confidence(), promote() -> Result, discard()
- [ ] `Relatable: Entity` — relations(&Graph) -> Vec<Relation>, graph_label() -> &'static str
- [ ] `Taggable: Entity` — tags() -> &[Tag], add_tag(Tag), remove_tag(&str)
- [ ] `ContentBearing: Entity` — content() -> &str, set_content(String), content_path() -> PathBuf
- [ ] Error types: TransitionError, PromotionError in error.rs
- [ ] `cargo build` succeeds

## Implementation Notes

### Dependencies
Depends on: CLO-T-0001 (scaffold), CLO-T-0002 (types)

### Technical Approach
Relatable takes `&Graph` — clotho-core depends on graph as a core concept. The Relation type referenced in Relatable's return needs a basic definition (can be fleshed out when clotho-graph work begins).

## Status Updates

- 7 entity traits defined in traits.rs: Entity, Activatable, Taskable, Extractable, Relatable, Taggable, ContentBearing
- Error types in error.rs: TransitionError, PromotionError, ClothoError
- Graph placeholder types: Graph (struct), Relation (struct), RelationType (enum with all 12 relation kinds from CLO-S-0005 + 3 temporal)
- `cargo build` succeeds