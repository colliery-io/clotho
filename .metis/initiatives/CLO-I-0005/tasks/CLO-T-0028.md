---
id: clotho-cli-crate-scaffold-clap
level: task
title: "clotho-cli crate scaffold + clap structure"
short_code: "CLO-T-0028"
created_at: 2026-03-17T02:14:34.389599+00:00
updated_at: 2026-03-17T02:22:24.024316+00:00
parent: CLO-I-0005
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: CLO-I-0005
---

# clotho-cli crate scaffold + clap structure

*This template includes sections for various types of tasks. Delete sections that don't apply to your specific use case.*

## Parent Initiative

[[CLO-I-0005]] — clotho-cli

## Objective

Create the clotho-cli crate with clap derive API, subcommand enum, global --json flag, and main.rs dispatch to stub handlers.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `clotho-cli/Cargo.toml` exists with dependencies: clap (with derive feature), clotho-core, clotho-store, serde_json
- [ ] clotho-cli is added to the workspace members in the root Cargo.toml
- [ ] `Cli` struct defined with clap derive, including global `--json` flag
- [ ] `Commands` enum with variants: Init, Ingest, List, Search, Query, Reflect
- [ ] `main.rs` dispatches each subcommand to a stub handler that prints "not yet implemented"
- [ ] `cargo build` succeeds with no errors

## Implementation Notes

### Dependencies
None — this is the first task in the clotho-cli initiative.

## Status Updates

- clotho-cli crate with clap 4 derive API
- Cli struct with global --json flag, Commands enum with 6 subcommands
- Stub handlers for each command (todo!() placeholders)
- main.rs dispatches to command handlers, formats errors as JSON when --json
- `cargo build` succeeds