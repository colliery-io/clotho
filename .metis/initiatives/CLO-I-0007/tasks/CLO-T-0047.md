---
id: syncengine-init-and-open
level: task
title: "SyncEngine: init and open"
short_code: "CLO-T-0047"
created_at: 2026-03-17T13:26:45.579333+00:00
updated_at: 2026-03-17T13:31:50.253533+00:00
parent: CLO-I-0007
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: CLO-I-0007
---

# SyncEngine: init and open

## Parent Initiative

[[CLO-I-0007]] — Git Sync Layer

## Objective

Implement `SyncEngine::init` (creates git repo in workspace parent, writes `.gitignore`) and `SyncEngine::open` (opens existing repo).

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `SyncEngine` struct wrapping `git2::Repository`
- [ ] `init(workspace_path)` creates git repo at `.workspace` parent dir (or uses existing)
- [ ] `init` writes `.gitignore` with `index/` entry
- [ ] `open(workspace_path)` opens existing repo or returns `NoRepository` error
- [ ] `has_remote()` checks if "origin" remote exists
- [ ] `cargo build` succeeds

## Implementation Notes

### Dependencies

- [[CLO-T-0046]] — clotho-sync crate scaffold

## Status Updates

- SyncEngine::init creates/opens git repo at workspace parent, writes .gitignore with index/ exclusion
- SyncEngine::open opens existing repo, errors if no .git
- has_remote() checks for "origin"
- `cargo build` succeeds