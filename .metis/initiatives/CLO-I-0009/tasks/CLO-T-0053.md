---
id: refactor-workspace-init-for
level: task
title: "Refactor Workspace::init for visible content layout"
short_code: "CLO-T-0053"
created_at: 2026-03-17T15:21:55.998821+00:00
updated_at: 2026-03-17T15:25:35.468919+00:00
parent: CLO-I-0009
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: CLO-I-0009
---

# Refactor Workspace::init for visible content layout

*This template includes sections for various types of tasks. Delete sections that don't apply to your specific use case.*

## Parent Initiative

[[CLO-I-0009]] — Visible Content Layout + Inbox

## Objective

Change `Workspace::init` to create visible content directories at project root and machine data in `.clotho/`.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `Workspace::init` creates at project root: `programs/`, `responsibilities/`, `objectives/`, `workstreams/`, `tasks/`, `meetings/`, `reflections/`, `artifacts/`, `notes/`, `people/`
- [ ] `Workspace::init` creates at `.clotho/`: `data/`, `graph/`, `index/`, `inbox/`, `config/`
- [ ] No longer creates `.clotho/content/` directory
- [ ] `Workspace` struct gains `project_root()` method returning parent of `.clotho/` path
- [ ] `Workspace::open` validates both `.clotho/` and visible dirs exist
- [ ] Default config files still written to `.clotho/config/`
- [ ] `cargo build` succeeds

## Implementation Notes

### Dependencies

None — this is the foundational task for the visible content layout initiative.

## Status Updates

- Workspace::init creates 11 visible dirs at project root (programs/, responsibilities/, objectives/, workstreams/, tasks/, meetings/, reflections/, artifacts/, notes/, people/, derived/)
- Creates 5 hidden dirs in .clotho/ (data/, graph/, index/, inbox/, config/)
- No more .clotho/content/ directory
- project_root() method returns parent of .clotho/ path
- Workspace::open validates .clotho/ structure (data, graph, config)
- inbox_path() accessor added
- content_path() removed (ContentStore will use project_root)
- `cargo build` succeeds