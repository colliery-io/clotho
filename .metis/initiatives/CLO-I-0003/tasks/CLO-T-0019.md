---
id: clotho-store-crate-scaffold
level: task
title: "clotho-store crate scaffold + workspace init"
short_code: "CLO-T-0019"
created_at: 2026-03-17T01:36:16.827355+00:00
updated_at: 2026-03-17T01:48:21.625099+00:00
parent: CLO-I-0003
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
initiative_id: CLO-I-0003
---

# clotho-store crate scaffold + workspace init

*This template includes sections for various types of tasks. Delete sections that don't apply to your specific use case.*

## Parent Initiative

[[CLO-I-0003]]

## Objective

Create the clotho-store crate, add it to the workspace, and implement `Workspace::init(path)` which creates the full `.workspace/` directory tree with default config files.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `clotho-store/Cargo.toml` with deps: clotho-core, rusqlite (with bundled + FTS5 features), serde, serde_json, toml, thiserror
- [ ] Added to workspace Cargo.toml members
- [ ] Module structure: `src/lib.rs`, `src/workspace.rs`, `src/content.rs`, `src/data.rs`, `src/index.rs`, `src/federation.rs`, `src/sync.rs`, `src/error.rs`
- [ ] `Workspace` struct with `path: PathBuf` field
- [ ] `Workspace::init(path) -> Result<Self, StoreError>` creates: content/{meetings,reflections,artifacts,notes,people}, data/, graph/, index/, config/
- [ ] Default `config/config.toml` written with sensible defaults
- [ ] Default `config/ontology.toml` written with empty known entities
- [ ] `Workspace::open(path) -> Result<Self, StoreError>` opens existing workspace, validates structure
- [ ] `StoreError` enum in error.rs
- [ ] `cargo build` succeeds

## Implementation Notes

### Dependencies
None — first task in CLO-I-0003.

## Status Updates

- clotho-store crate created with deps: clotho-core, rusqlite 0.31 (bundled), serde, serde_json, toml, thiserror, tempfile (dev)
- Module stubs: lib.rs, workspace.rs, content.rs, data.rs, index.rs, federation.rs, sync.rs, error.rs
- Workspace::init() creates full .workspace/ tree: content/{meetings,reflections,artifacts,notes,people}, data/, graph/, index/, config/
- Default config.toml and ontology.toml written
- Workspace::open() validates directory structure
- StoreError enum with 10 variants
- Note: rusqlite 0.31 because graphqlite pins that version
- `cargo build` succeeds